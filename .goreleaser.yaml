version: 2

project_name: cc-dailyuse-bar

builds:
  - id: cc-dailyuse-bar
    main: ./src
    binary: cc-dailyuse-bar
    env:
      - CGO_ENABLED=1
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
    ignore:
      # Cross-compiling CGo between OS/arch requires specific toolchains.
      # Build native-only for now; CI matrix handles cross-platform.
      - goos: linux
        goarch: arm64

archives:
  - id: default
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"

# macOS code signing (requires Developer ID certificate)
# Uncomment when certificate is available:
#
# signs:
#   - id: darwin
#     cmd: codesign
#     args:
#       - "--deep"
#       - "--force"
#       - "--options=runtime"
#       - "--entitlements=packaging/macos/entitlements.plist"
#       - "--sign"
#       - "Developer ID Application: YOUR_NAME (TEAM_ID)"
#       - "{{ .Path }}"
#     artifacts: binary

# macOS notarization (requires Apple Developer credentials)
# Uncomment when ready:
#
# notarize:
#   - macos:
#       - enabled: true
#         ids:
#           - cc-dailyuse-bar
#         notarytool:
#           keychain_profile: CC_DAILYUSE_BAR

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
